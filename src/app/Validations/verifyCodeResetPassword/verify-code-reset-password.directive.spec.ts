import { VerifyCodeResetPasswordDirective } from './verify-code-reset-password.directive';

describe('VerifyCodeResetPasswordDirective', () => {
  it('should create an instance', () => {
    const directive = new VerifyCodeResetPasswordDirective();
    expect(directive).toBeTruthy();
  });
});
