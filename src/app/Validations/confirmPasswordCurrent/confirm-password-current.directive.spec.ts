import { ConfirmPasswordCurrentDirective } from './confirm-password-current.directive';

describe('ConfirmPasswordCurrentDirective', () => {
  it('should create an instance', () => {
    const directive = new ConfirmPasswordCurrentDirective();
    expect(directive).toBeTruthy();
  });
});
